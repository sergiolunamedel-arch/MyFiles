Fix inmediato — restaura el id de QA que viste en la captura original antes de pegar el XML de prod:

Abre el XML que tienes pegado en el editor y cambia la línea 3 de:

xml
<ObjectConfig name="Identity">

a:

xml
<ObjectConfig id="0a32315395c816138195c8ed2659011e" name="Identity">

(Ese es el id que QA ya tenía asignado para este objeto — lo vimos en tu captura anterior antes de sobreescribir.)

Antes de guardar, hay un segundo problema que sí va a tronar (o va a quedar mal silenciosamente) si no lo arreglas ahora: cada uno de los ~50 bloques <ObjectAttribute> del XML de producción trae su <ApplicationRef> apuntando a:

xml
<Reference class="sailpoint.object.Application" id="0a32315395cf1fac8195d031ebe30254" name="BancoBase-Application-IntelexionWebService"/>

Ese id y ese name son los de producción. En QA tu app se llama distinto y tiene otro id — por la URL que vimos antes (appId=0a323693a0b5123d81a0c51dc43f3334) y el nombre Intelexion - QA. Si guardas tal cual, esas referencias no van a resolver contra tu app real de QA.

Corrígelo con un reemplazo global antes de pegar/guardar (hazlo en un editor de texto local, no dentro del textarea del navegador — más seguro y puedes deshacer):

bash
sed -i \
  -e 's/id="0a32315395cf1fac8195d031ebe30254" name="BancoBase-Application-IntelexionWebService"/id="0a323693a0b5123d81a0c51dc43f3334" name="Intelexion - QA"/g' \
  identity_objectconfig_prod.xml

Además, este XML tiene una referencia a otra app (BancoBase-Application-DirectorioActivo, usada solo para el atributo email) — es tu AD, que en QA se llama Active Directory - QA con otro id. Antes de guardar:

Abre esa app en QA, mira la URL (appId=...) igual que hiciste con Intelexion.
Agrega otra línea sed con ese id/nombre real.

Y una tercera cosa a vigilar, no a resolver todavía: hay tres <RuleRef> (BancoBase-IdentityMap-Inactive, BancoBase-identityMap-type, BancoBase-IdentityMap-MX) que casi seguro no existen en QA. Guarda primero con los dos reemplazos de arriba y mira si el Save pasa o si el log se queja de estos Rules. Si truena por eso, dímelo y estos tres Rules se exportan/importan individualmente igual que el ObjectConfig (Object Browser → Class Rule → buscar por nombre en prod → copiar XML → crear en QA).

¿Ya tienes el appId de tu Active Directory en QA para armar el segundo sed, o lo buscamos juntos?